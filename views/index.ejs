<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bookstore</title>
</head>
<body>
<a href="/">Home</a>
<a href="/admin">Admin page</a>

<h1>Bookstore Inventory</h1>

<!-- Display the list of books -->
<ul>
    <% books.forEach(book => { %>
        <li>
            <%= book.title %> by <%= book.author %> - <%= book.price %> USD
        </li>
    <% }); %>
</ul>

</body>
</html>
